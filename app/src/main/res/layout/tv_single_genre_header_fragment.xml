<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/primaryColor"
    android:padding="@dimen/fragment_padding"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/single_genre_title_poster"
        android:layout_width="300dp"
        android:layout_height="200dp"
        tools:src="@drawable/placeholder_movie_landscape"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/background_top_title_cover_tv"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/single_genre_title_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Title Name"
        android:textSize="20sp"
        android:textColor="@color/white"
        app:layout_constraintWidth_default="percent"
        app:layout_constraintWidth_percent="0.8"
        app:layout_constraintTop_toTopOf="@id/single_genre_title_poster"
        app:layout_constraintStart_toStartOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>